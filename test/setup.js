// test/setup.js

// Add global test setup here
import "@testing-library/jest-dom"; // for DOM matchers if you use RTL

// Example: mock fetch if needed
// global.fetch = (...args) => import("node-fetch").then(({default: fetch}) => fetch(...args));
